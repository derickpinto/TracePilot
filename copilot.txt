Update the mock data sending logic as below:
Right a script that sends mock event to kafka topics every 5seconds.
Generate transaction id events everytime, transaction event muust have same transaction id.

use local kafka setup as given in docker-compose of local kafka 
Use mongosh of local mongo setup to verify the events are coming in. and store it.